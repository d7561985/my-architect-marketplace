{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/code-graph-context.sh\"",
            "async": false,
            "timeout": 10
          },
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/architect-health.sh\"",
            "async": false,
            "timeout": 10
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "mcp__plugin_my-architect_my-architect__complete_task",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'my_architect: complete_task changed node statuses only. Finish Workflow A now: explicitly mark only proven requirements done, read fresh get_issues, close an issue only when every link in its nonempty closedBy is done, then refresh the derived index and validate. Keep partial solutions open; approved is not done. Next run the scan-for-gaps pass: re-read commit body AND this chat turn, dispatch debt-scanner for every deferred/caveat/known issue, and summarize actual requirement/issue status, validation and tracked gaps.'"
          }
        ]
      }
    ]
  }
}
